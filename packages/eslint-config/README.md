# @aenjojo/eslint-config

## Install
```sh
yarn add @aenjojo/eslint-config --dev
```

## Usage
add `.eslintrc.json` (or `.eslintrc.cjs`), then add the following in the extends property.
```json
{
	"extends": ["@aenjojo"]
}
```